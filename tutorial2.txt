MailMessage mesaj = new MailMessage();
            mesaj.Subject = textBox1.Text;
            mesaj.From = new MailAddress("x@gmail.com");
            mesaj.To.Add(textBox2.Text);
            mesaj.Body = textBox3.Text;

            SmtpClient server = new SmtpClient("smtp.google.com");
            server.Credentials = new System.Net.NetworkCredential("x@gmail.com", "password");
            server.Port = 587;

            server.Send(mesaj);
            MessageBox.Show("Gönderildi");
